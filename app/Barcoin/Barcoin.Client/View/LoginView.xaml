<UserControl x:Class="Barcoin.Client.View.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:Dialog="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
             mc:Ignorable="d"
             DataContext="{Binding Source={StaticResource Locator}, Path=Login}"
             Style="{StaticResource global}"
             Dialog:DialogParticipation.Register="{Binding}"
             Height="720"
             Width="1200"
             d:DesignHeight="720" d:DesignWidth="1200">
    <Grid HorizontalAlignment="Center" Margin="0 30 0 180">
        <Grid.RowDefinitions>
            <RowDefinition Height=".7*"/>
            <RowDefinition Height=".4*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" FontSize="40" TextAlignment="Center" Text="Login" Width="300"/>
        <Separator Grid.Row="1" Background="DeepSkyBlue" />
        <TextBox Text="{Binding Path=Username, Mode=TwoWay}" Grid.Row="2" Height="35" Width="280" FontWeight="Bold" Controls:TextBoxHelper.Watermark="Insert username" />
        <PasswordBox Name="pass" PasswordChar="*" Grid.Row="3" Height="35" Width="280" FontWeight="Bold" Controls:TextBoxHelper.Watermark="Insert password" />
        <Border Grid.Row="6" Height="50" Width="280" BorderBrush="Red" BorderThickness="2" Visibility="{Binding Path=ErrorVisibility}">
            <Grid Background="#20FF0000">
                <TextBlock Text="{Binding Path=Error}" Margin="5" TextWrapping="WrapWithOverflow" VerticalAlignment="Center" TextAlignment="Center"></TextBlock>
            </Grid>
        </Border>
        <Button Grid.Row="7" Margin="10" Height="30" Width="130" HorizontalAlignment="Left" Content="Sign in" Command="{Binding Path=LoginCommand}" CommandParameter="{Binding ElementName=pass}"></Button>
        <Button Grid.Row="7" Margin="10" Height="30" Width="130" HorizontalAlignment="Right" Content="Register" Command="{Binding Path=GotoRegisterCommand}"></Button>
    </Grid>
</UserControl>
